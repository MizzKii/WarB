public enum ResourcesName{
	Stone,Wood
}

public enum Direction{
	back=-1, none=0, forward=1, left=2, right=3
}

public enum Compass{
	north = 0, east = 90, south = 180, west = 270
}

public enum GameState{
	Index, Single, Multi, Lobby, How, GamePlay, Error, EndGame
}

public enum Map{
	Classic = 1, Rectangle = 2, Plus = 3
}